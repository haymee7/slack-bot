plugins {
    id 'java'
}

group 'io'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'

    implementation group: 'com.slack.api', name: 'slack-api-client', version: '1.8.1'
}
